script.
	import { getContext } from 'svelte'

	import { apply_query_parameters } from '../../lib/url/index'

	import Checkbox from '../generic/Checkbox'

	export filter = null

	internal_navigate = getContext('internal_navigate')

Checkbox(action={() => internal_navigate({ href: apply_query_parameters({ filter: filter === 'unread' ? 'all' : 'unread' }) })} checked={filter === 'all'} text='Show read')