script.
	import { startLogin, logout } from '../api/login.coffee'

{#if LS.userKey}
	button(on:mousedown={() => logout()})
		img.user-img(src={LS.userPic} alt='')
{:else}
	button(on:mousedown={() => startLogin()}) Log in

style.
	button
		height 48px
		padding 2rem
		display flex
		align-items center
	img
		height 64px
		width 64px