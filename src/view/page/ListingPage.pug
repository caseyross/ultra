script.
	export page = null
	export selection = ''
	import ListingItem from './ListingItem.pug'
	$: ordinal = (submission) -> page.submissions.indexOf(submission) + 1

ol.list-none
	{#each page.submissions as submission}
		li
			.ordinal {ordinal(submission)}
			ListingItem(submission={submission} selected={selection === submission.id})
			.arrow ►

style.
	li
		display grid
		grid-template-columns 10rem 1fr 10rem
	.ordinal
		width 8rem
		height 8rem
		background var(--black)
		color white
		border-radius 50%
		display flex
		justify-content center
		align-items center
		font-size 16px
	.arrow
		opacity 0
		.selected &
			opacity 1