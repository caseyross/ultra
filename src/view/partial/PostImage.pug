script.
	export image = []

figure
	a(href={image.biggest().href} data-layer-type='image')
		img(src={image.dimension_target({ width: 640, height: Infinity }).href} alt='')
	{#if image.caption.text || image.caption.href}
		figcaption
			{#if image.caption.text}
				| {image.caption.text}
			{#if image.caption.href}
				a(href={image.caption.href}) {image.caption.href}

style.
	figure
		position relative
		margin-right 1px
	figcaption
		position absolute
		top 0
		left 0
		padding 2rem
		background white
		border 1px solid
		border-radius 2rem
		word-break break-word
	img
		box-sizing content-box
		max-width 640px
		border 1px solid
		cursor zoom-in