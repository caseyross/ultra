script.
	export images = []
	import Image from './Image'

section
	{#each images as image}
		Image(data={image} minWidth={640} maxWidth={960} enableZoom={true})

style.
	section
		display flex
		flex-flow column nowrap
		align-items flex-end