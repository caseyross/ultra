script.
	export comments = []
	import Comment from '../comment/Comment'

{#if comments.length === 0}
	span [No comments.]
{:else}
	{#each comments as top_level_comment}
		Comment(comment={top_level_comment})