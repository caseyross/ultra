script.
	import UserFlair from './UserFlair.pug'

	export distinguish = null
	export flair_color = null
	export flair_rich_text = null
	export flair_text = null
	export name = null

span.user-byline(class='user-byline-distinguish-{distinguish}')
	a.user-byline-name(href='/u/{name}') {name}
	{#if flair_text}
		UserFlair(color={flair_color} emoji_style='image' rich_text={flair_rich_text} text={flair_text})

style.
	.user-byline
		align-items center
		color var(--c-text-weak)
		display flex
		font-size 12px
		letter-spacing 0.666px
		line-height 1
		gap 1ch
		max-width 100%
		padding 1px 7px
		white-space nowrap
	.user-byline-name
		font-weight bold
		.user-byline-distinguish-admin &
			color var(--c-author-admin)
			font-weight var(--f-wght-strong)
		.user-byline-distinguish-moderator &
			color var(--c-author-mod)
			font-weight var(--f-wght-strong)
		.user-byline-distinguish-special &
			color var(--c-author-vip)
			font-weight var(--f-wght-strong)
		.user-byline-distinguish-submitter &
			color var(--c-author-op)
			font-weight var(--f-wght-strong)