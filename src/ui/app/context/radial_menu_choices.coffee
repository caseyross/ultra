import api from '../../../api/index.js'

export default {

	comment:

		0: (dataset) ->

		1: (dataset) ->

		2: (dataset) ->

		3: (dataset) ->

		4: (dataset) ->

	post:

		0: (dataset) ->
		
		1: (dataset) ->

		2: (dataset) ->

		3: (dataset) ->

		4: (dataset) ->

}