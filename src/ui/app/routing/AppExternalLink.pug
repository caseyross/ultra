script.
	import CenterLayout from '../../common/layout/CenterLayout.pug'
	export original_url = null
	redirect_url = new URL(original_url)
	redirect_url.protocol = 'https:'
	redirect_url.hostname = 'www.reddit.com'
	redirect_url.port = ''

CenterLayout
	h1 This page isn't supported yet.
	a#external-link(href={redirect_url}) View page on reddit.com

style.
	#external-link
		border 1px solid
		border-radius 10px
		padding 1em