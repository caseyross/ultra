script.
	import { startLogin, logout } from '../libraries/api/account.coffee'

{#if LS.userKey}
	button(on:click={() => logout()})
		img.user-img(src={LS.userPic} alt='')
{:else}
	button(on:click={() => startLogin()}) Log in

style.
	button
		height 64px
		padding 8px
		display flex
		align-items center
	img
		height 64px
		width 64px